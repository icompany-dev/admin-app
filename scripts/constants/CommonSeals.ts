export enum CommonSealStoredAt {
  RegisteredOffice = "registered-office",
  BusinessAddress = "business-address",
}

export enum CommonSealAuthorityOf {
  BoardOfDirectors = "board",
  SpecificDirector = "specific",
}

export enum CommonSealReplacementReason {
  Lost = "lost",
  Damaged = "damaged",
}

export enum CommonSealReplacementNullifiedFrom {
  ResolutionDate = "resolution-date",
  CustomDate = "custom-date",
}
